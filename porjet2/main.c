#include <stdio.h>
#include <stdlib.h>
int nombre;
int note;
int main()
{
    nombre == 0;

    while (nombre < 30){
        printf("entrez votre %d note:",nombre);
        scanf("%d",&note );
        nombre = nombre + 1;
        printf("%d\n", note);

    }

    return 0;
}
