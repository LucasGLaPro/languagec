#include <stdio.h>
#include <stdlib.h>
    int prix;
int main()
{
    printf("entrez une valeur: ");
    scanf("%d",&prix );
    if(prix > 500)
    {
        printf("c'est %d", prix = prix * 0.9);
    }
    else
    {
           printf("c'est %d", prix);
    }

    return 0;
}
